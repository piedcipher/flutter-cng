name: flutter_change_notifier_gen
description: Tool to generate Flutter Change Notifiers.
publish_to: 'none'
version: 2.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  flutter_highlight: ^0.7.0
  google_fonts: ^2.1.0
  cupertino_icons: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
